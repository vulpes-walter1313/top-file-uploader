<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("partials/head") %>
  <script src="/javascript/folderCreateInputCounter.js" defer></script>
</head>
<body class="bg-slate-50">
  <%- include("partials/navbar") %>
  <section class="p-4 flex flex-col gap-10 py-14 items-center justify-center bg-slate-50 md:py-20">
    <div class="flex flex-col gap-6">
      <h1 class="text-2xl font-bold text-slate-950 md:text-4xl"><%= locals.h1 %></h1>
    </div>
    <form action="<%- locals.action %>" method="POST" class="p-6 flex flex-col gap-4 w-full max-w-sm bg-white border border-slate-300 shadow-lg rounded-xl">
      <div class="flex flex-col gap-1 relative">
        <label for="name" class="text-slate-800">Folder Name <span class="text-xs text-slate-600">(30 characters)</span></label>
        <input 
          type="text"
          name="name"
          id="name"
          value="<%= locals.name ? locals.name : "" %>"
          class="text-slate-900 rounded-md text-lg bg-slate-50 border border-slate-300  placeholder:text-slate-500 px-4 py-2"
          />
          <p class="absolute bottom-0 right-2 text-slate-600 text-xs" id="folder-name-counter">0/50</p>
      </div>
      <% if (locals.validErrors?.name) { %>
        <p class="p-2 bg-red-50 border border-red-100 text-red-900 rounded-md"><%= locals.validErrors.name?.msg%></p>    
      <% } %>
      <div class="flex flex-col gap-1 relative">
        <label for="description" class="text-slate-800">Folder Description <span class="text-xs text-slate-600">(256 characters)</span></label>
        <textarea 
          type="text"
          name="description"
          id="description"
          class="text-slate-900 h-32 rounded-md text-lg bg-slate-50 border border-slate-300  placeholder:text-slate-500 px-4 py-2"><%- locals.description ? locals.description : null %></textarea>
        <p class="absolute bottom-0 right-2 text-slate-600 text-xs" id="folder-description-counter">0/256</p>
      </div>
      <% if (locals.validErrors?.description) { %>
        <p class="p-2 bg-red-50 border border-red-100 text-red-900 rounded-md"><%= locals.validErrors.description?.msg%></p>    
      <% } %>
      <button type="submit" class="bg-slate-800 text-slate-50 text-lg px-6 py-2 rounded-xl block leading-none"><%= locals.submitText %></a>
    </form>
  </section>
</body>
</html>